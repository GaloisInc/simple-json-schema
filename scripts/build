#/bin/bash

set -e

NAME=simple-json-schema
cabal build $NAME
mkdir -p bin
VERSION="$(scripts/version)"
BIN="$NAME-$VERSION"
if [ "$OS_TAG" != "" ]; then BIN="$BIN-$OS_TAG"; fi
if [ "$ARCH_TAG" != "" ]; then BIN="$BIN-$ARCH_TAG"; fi
cp $(cabal list-bin -v0 $NAME) "$BIN"
echo $BIN

